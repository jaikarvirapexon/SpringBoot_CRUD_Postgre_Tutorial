# Validation report — STU-01

- Date: 2026-08-12T22:19:05Z
- Verdict: PASS
- Mode: full (fresh run of `/arh-validate-feature STU-01`, no `--rerun`/`--rerun-failed` flag; distinct from the `/arh-implement` Step 2 validation reported earlier the same day at 2026-08-12T22:20 in `VALIDATION-20260812-2220.md`)
- Total: 5   Passed: 5   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~300 seconds (preflight through report write)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   1   |   1  |   0  |   0   |   0   |
| Unit + Web-slice (regression, `./mvnw test`) | 8 | 8 | 0 | 0 | 0 |
| Integration  |   4   |   4  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

Stack-smoke row: `docs/config/stack-smoke.md` declares one section, `spring-boot-web`. `Run: ./mvnw spring-boot:run` was used (the `Docker:` bullet is `(n/a — no Dockerfile in this repo)`). PASS.

Integration + Security rows are the five behavioural TCs from `docs/test-cases/STU-01.json` (STU-01-TC-01..05), executed as real HTTP calls against the live server — no mocks at the integration boundary.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `src/main/java/com/example/demo/student/StudentService.java`, `src/test/java/com/example/demo/student/StudentServiceTest.java` (Add `getStudentById` + unit test) | ✓ | ✓ (`getStudentById` method present in both files; `getStudentById_idExists_returnsOptionalOfStudent` / `getStudentById_idAbsent_returnsOptionalEmpty` test methods present) | PASS |
| T-02 | `src/main/java/com/example/demo/student/StudentController.java`, `src/test/java/com/example/demo/student/StudentControllerTest.java` (Add GET-by-id handler + web-slice test) | ✓ | ✓ (`@GetMapping(path = "{studentId}")` present; `@WebMvcTest(StudentController.class)` slice with 200/404/400/negative-id/no-stack-trace test methods present) | PASS |
| T-03 | `README.md` (Document the new endpoint in README) | ✓ | ✓ (`### Get a single student by ID` section present at line 529, with `.http`-style `GET http://localhost:8080/api/v1/student/1` request/response example) | PASS |

All 3/3 PLAN.md § 5 Task Breakdown rows verified complete on disk.

### Passed

| TC id            | Title                                                                          |
|------------------|---------------------------------------------------------------------------------|
| STU-01-TC-01     | GET /api/v1/student/{studentId} returns 200 with full student body             |
| STU-01-TC-02     | GET /api/v1/student/{studentId} returns 404 (not 500) when id does not exist   |
| STU-01-TC-03     | GET /api/v1/student/{studentId} with non-numeric path segment returns 400      |
| STU-01-TC-04     | GET /api/v1/student/{studentId} with negative/zero id returns 404, not error   |
| STU-01-TC-05     | 404 response body contains no stack trace or internal exception identifier    |

### Regression test suite (independent re-run)

`./mvnw test` (wrapper — working this run, no fallback to system `mvn` needed):

```
Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

8/8 green — no regression from the cached `/arh-implement` Step 2 validation earlier the same day.

### Proof of run

```
### Proof of run

- session_id:        1921e9f2-e3f9-4fa9-b1c7-8847a6c23d52
- bash_invocations:  23
- test_runner_runs:  7  (5 TC integration flows executed against the live server via tests/integration/STU-01-TC-0{1..5}.sh; 1 independent `./mvnw test` regression run; 1 extra STU-01-TC-01 execution from a first attempt that aborted on a shell-arithmetic bug in the harness loop before the remaining 4 TCs ran, not a TC failure — re-run cleanly immediately after)
- stack_smoke_runs:  1  (spring-boot-web: start via `./mvnw spring-boot:run`, poll `http://127.0.0.1:8080/api/v1/student` until 200, stop)
- duration_seconds:  ~300
- timestamp:         2026-08-12T22:19:05Z
```

### Environment notes (non-blocking)

- A stale `DemoApplication` Java process (PID 27789, elapsed 12:35) was found squatting on port 8080 before this run started — leftover from an earlier session that did not clean up its background server. Per `validation-execution` § Environment check, this was surfaced and killed (not silently reused) before starting a fresh stack-smoke boot for this run, so the smoke PASS above reflects this run's own boot, not the stale process.
- `./mvnw` (Maven wrapper) worked end-to-end this run — `preflight`, `spring-boot:run`, and `test` all ran via `./mvnw` with no fallback to system `mvn` needed. This confirms the `.mvn/wrapper/` restoration (carry-forward `AF-01-carry`, resolved 2026-08-12T22:00:35Z) is holding.
- Local Postgres was already up and reachable (`pg_isready` on 127.0.0.1:5432) with the `student` database present; no Postgres start/provisioning step was required (`docs/config/stack-smoke.md` `Deps:` bullet's `[NEEDS CLARIFICATION]` placeholder remains unaddressed but was not blocking since a usable local instance already existed — same gap noted in the prior validation run).
